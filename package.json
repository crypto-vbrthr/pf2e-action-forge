{
  "name": "pf2e-action-forge",
  "version": "0.1.0-dev.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check scripts/action-forge.js && node --check scripts/core/action-registry.js && node --check scripts/core/actor-resolver.js && node --check scripts/ui/action-forge-app.js && node --check scripts/ui/scene-controls.js"
  }
}
